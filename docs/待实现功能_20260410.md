# 待实现功能

> 创建日期：2026-04-10

---

## 1. Mock Atom SOAP 接口实现

**问题描述**：
- Real Atom 已完成联调，支持所有 SOAP 接口
- Mock Atom 需要实现相同接口，将 SOAP 转发到 rmcp_proxy

**需要实现**（11 个接口）：
- B_QueryDeviceInfo, B_QueryFaciDevStat, B_StopMeas
- B_SglFreqMeas, B_SglFreqDF, B_FScan, B_FScanDF
- B_MScan, B_MScanDF, B_PScan, B_WBDF

**参考**：`test_interface_sequence.py` 接口定义

**优先级**：高

---

## 2. Mock Atom RMCPTP 转发

**问题描述**：
- Mock Atom 需要将 SOAP 请求转换为 RMCPTP 帧
- 转发到 rmcp_proxy (9996)
- 接收响应并转换回 SOAP

**需要实现**：
1. RMCPTPFrameBuilder - 构建 RMCPTP 请求帧
2. RMCPTPResponseParser - 解析 RMCPTP 响应帧
3. TCP 客户端 - 连接 rmcp_proxy

**参考**：`capture_20260410_231857.log` RMCPTP 帧格式

**优先级**：高

---

## 3. Mock Atom 监听 streamsrc 功能

**问题描述**：
- Real Atom 的 streamsrc (18012) 是 TCP 服务器，等待设备连接
- Mock Atom 当前没有实现 streamsrc 监听功能

**需要实现**：
1. Mock Atom 添加 streamsrc 监听模块
2. 在 18012 端口监听设备连接

**替代方案**：
- 使用远程设备（100.89.170.72:9997）代替 streamsrc

**优先级**：中

---

## 4. 全链路测试自动化

**问题描述**：
- 当前需要手动启动多个服务
- 需要一键启动完整测试链路

**优先级**：中
