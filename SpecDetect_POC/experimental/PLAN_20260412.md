# 下一步方案 - 2026-04-12

## 背景

soap_to_rmcp_direct 直连设备测试失败:
- TCP 连接成功
- RMCP 帧格式正确 (与 Atom 一致)
- 设备返回 `RMTP:ErrCode=-1` 拒绝请求

## 方案A: tcpdump 抓包分析

### 目标
确定设备拒绝直连的真正原因

### 执行步骤

1. **准备抓包**
   ```bash
   # 在能访问设备的机器上执行
   tcpdump -i any host 100.72.95.36 and port 1449 -w /tmp/direct_test.pcap
   ```

2. **触发直连测试**
   ```bash
   cd D:/arvin/claude_workspace/SpecDetect_V4/SpecDetect_POC/experimental
   python soap_to_rmcp_direct.py
   ```

3. **触发正常测试 (通过 rmcp_proxy)**
   ```bash
   # 通过 TestTool 发起正常请求，rmcp_proxy 会捕获
   ```

4. **对比分析**
   - direct_test.pcap vs rmcp_proxy/capture/*.pcap
   - 找出 TCP 层面的差异 (flags, options, seq/ack 等)

### 预期产出
- 确定是 TCP 层面还是应用层被拒绝
- 如果是 TCP 层面，可能是防火墙/ACL
- 如果是应用层，可能是 RMCP 协议版本/标志差异

### 风险
- 如果无法在设备端抓包，此方案不可行
- 需要设备侧配合

---

