# 架构解耦重构任务列表 [已归档]

**创建日期**: 2026-04-22
**归档日期**: 2026-04-25
**状态**: 已完成 — 实际采用 rmcp/ + stream/ + soap/ 目录结构

---

## 背景

当前 `fscan_processor.py` 职责过重，耦合了 MSCAN/PScan/FSCAN 三种帧生成，且频谱编码方式与 emulated_atom.py 不一致。

**参考文档**: `ARCHITECTURE_RMCP_STREAMSRC_解耦设计.md`

---

## 任务列表

### P0 (阻塞性问题)

#### P0-1: 频谱编码方式修正
**问题**: 当前 spectrum_data 编码与 emulated_atom.py 不一致
- emulated_atom.py: 输入 dBm 值，直接 `byte = 256 + dBm`
- 当前实现: `levels_raw` 是 int16 (0.1dBm单位)，但直接打包为 int16

**操作**: 确定正确的频谱编码方式（需要对照真实设备抓包）

**状态**: 待定

---

### P1 (重要任务)

#### P1-1: 拆分 streamsrc_frame.py
**目标**: 将 MSCAN/PScan 相关函数从 fscan_processor.py 迁出

**操作**:
1. 创建 `src/atom/protocol/streamsrc_frame.py`
2. 迁移以下函数:
   - [ ] `_get_streamsrc_timestamp()`
   - [ ] `build_mscan_frame()`
   - [ ] `build_pscan_level_frame()`
   - [ ] `build_pscan_itu_frame()`
   - [ ] `build_pscan_spectrum_frame()`
3. 更新 import 引用

**验证**: 确保 MSCAN/PScan 功能正常

**状态**: 未开始

#### P1-2: RMCP Parser 分离
**目标**: 分离 RMCP 帧头解析和 Payload 解析

**操作**:
1. 创建 `src/atom/protocol/rmcp_parser.py`
2. 提取通用 RMCP 帧头解析函数
3. 分离 FSCAN/MSCAN/SGLFREQ payload 解析
4. 验证解析结果与真实抓包一致

**验证**: RMCP 数据解析正确性

**状态**: 未开始

#### P1-3: streamsrc_server.py 纯化
**目标**: 移除帧生成逻辑，只保留推送

**操作**:
1. 移除 `_push_frame` 中的帧生成代码
2. 改为调用 `streamsrc_frame.py` 中的函数
3. 确保推送回调干净

**验证**: 推送功能正常

**状态**: 未开始

---

### P2 (优化任务)

#### P2-1: FSCAN 帧验证
**目标**: 对照 emulated_atom.py 和真实抓包，验证 FSCAN 帧格式

**操作**:
1. 对比 emulated_atom.py 的 `build_streamsrc_frame()` 实现
2. 对照真实设备的 FSCAN 帧结构
3. 修正频谱编码方式

**状态**: 待定

#### P2-2: 文档更新
**目标**: 更新 ARCHITECTURE.md

**操作**:
1. 反映新的架构设计
2. 添加模块依赖图

**状态**: 未开始

---

## 任务依赖

```
P1-1 (streamsrc_frame拆分) 完成后
        ↓
P1-3 (streamsrc_server纯化) 可开始
        ↓
P1-2 (rmcp_parser分离) 可开始
        ↓
P2-1 (FSCAN帧验证) 可开始
```

---

## 当前问题记录

### 2026-04-22
- FSCAN 功能停止推送的原因未确定
- 频谱编码方式需要对照 emulated_atom.py 确认
- emulated_atom.py 的 spectrum_data 输入是 dBm 值
- SpecDetect_Atom 的 levels_raw 是 int16 (0.1 dBm单位)
